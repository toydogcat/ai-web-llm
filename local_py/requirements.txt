# Python dependencies for Gemma Local Chat
# ─────────────────────────────────────────────────────────────
# These are ALREADY installed in conda env `toby`:
#   streamlit, torch, transformers, httpx, openai
# Only install what's missing.
# ─────────────────────────────────────────────────────────────

# Required (for toby env — likely already present)
# streamlit>=1.46.0       ← already in toby ✓
# transformers>=5.0.0     ← already in toby ✓
# torch>=2.0.0            ← already in toby ✓

# Optional: llama.cpp Python binding (FASTEST on CPU, recommended)
# Install separately with:
#   conda run -n toby pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# llama-cpp-python>=0.2.90

# Optional: Ollama REST client (easy to install)
ollama>=0.4.0

# Optional: coloured terminal output for chat.py
colorama>=0.4.6
